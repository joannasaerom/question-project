
import { answerFire } from 'qna-project/helpers/answer-fire';
import { module, test } from 'qunit';

module('Unit | Helper | answer fire');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerFire([42]);
  assert.ok(result);
});

