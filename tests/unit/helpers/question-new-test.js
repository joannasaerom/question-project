
import { questionNew } from 'qna-project/helpers/question-new';
import { module, test } from 'qunit';

module('Unit | Helper | question new');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionNew([42]);
  assert.ok(result);
});

